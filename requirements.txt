# Telegram bot framework (21.x is API 8.x; v20.x still supported)
python-telegram-bot>=21.0

# HTTP async
aiohttp>=3.10.0

# HTTP sync (used for startup notification and restart)
requests>=2.31.0

# .env parsing (more robust than the manual parser)
python-dotenv>=1.0.0

# Real encryption (replaces the XOR+Base64 "crypto")
cryptography>=43.0.0

# Image processing (for vision features)
Pillow>=10.4.0

# Safe arithmetic evaluator (replaces eval in handle_calc)
# Falls back to a built-in shunting-yard parser if missing
simpleeval>=1.0.0

# Local Whisper fallback for voice transcription
# Comment this out if you don't need offline transcription
# openai-whisper>=20231117

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0